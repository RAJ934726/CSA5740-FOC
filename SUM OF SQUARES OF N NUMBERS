#include<stdio.h>
int main()
{
int n, sum=0;
printf("Enter any number: ");
scanf("%d", &n);
for(int i=0; i<=n; i++)
{
sum += (i*i);
}
printf("Sum of squares of first %d  numbers = %d",n, sum);
return 0;
}
